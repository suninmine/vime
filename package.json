{
  "name": "vime",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=12",
    "yarn": ">=1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "core": "yarn workspace @vime/core",
    "react": "yarn workspace @vime/react",
    "vue": "yarn workspace @vime/vue",
    "vue:next": "yarn workspace @vime/vue-next",
    "svelte": "yarn workspace @vime/svelte",
    "stencil": "yarn workspace @vime/stencil",
    "angular": "yarn workspace @vime/angular",
    "lint": "yarn workspaces run lint --parallel",
    "build": "yarn workspaces run build --parallel",
    "release": "lerna publish --yes"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "@typescript-eslint/eslint-plugin": "^4.6.0",
    "@typescript-eslint/parser": "^4.6.0",
    "eslint": "^7.8.1",
    "eslint-config-airbnb-typescript": "^12.0.0",
    "eslint-plugin-import": "^2.22.0",
    "husky": "^4.2.5",
    "lerna": "^3.22.1",
    "typescript": "^4.0.2"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
